import styled from '@emotion/styled';

const SidebarLeft = () => {
  return (
    <SidebarContainer>SidebarLeft</SidebarContainer>
  );
};

export default SidebarLeft;

const SidebarContainer = styled.div`
  width:100px;
  border-right:1px solid #000 
`;
