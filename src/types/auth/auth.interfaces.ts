export interface SignupFormValues {
  userName: string;
  userAge: string;
  userGender: string;
  userId: string;
  userPassword: string;
  userPasswordConfirm: string;
}

export interface LoginProps {
  email: string;
  password: string;
}
